<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Harshit & Kavita | Wedding Celebration</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Great+Vibes&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary-gold: #d4af37;
      --secondary-gold: #f3e5ab;
      --bg-dark: #0f1016;
      --card-bg: #181a24;
      --text-light: #f5f5f5;
      --text-muted: #a0a5b5;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      background-color: var(--bg-dark);
      color: var(--text-light);
      text-align: center;
      line-height: 1.6;
    }

    h1, h2, h3, .heading-font {
      font-family: 'Cinzel', serif;
      letter-spacing: 1px;
    }

    .cursive {
      font-family: 'Great Vibes', cursive;
      color: var(--primary-gold);
      font-size: 2.8rem;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* HERO BACKGROUND IMAGE - CHANGE URL BELOW TO CHANGE THE MAIN BACKGROUND */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background: linear-gradient(rgba(15, 16, 22, 0.75), rgba(15, 16, 22, 0.9)), 
                  url('https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1920&q=80') center/cover no-repeat;
      padding: 40px 20px;
    }

    .hero h3 {
      text-transform: uppercase;
      font-size: 0.9rem;
      letter-spacing: 3px;
      color: var(--primary-gold);
      margin-bottom: 10px;
    }

    .hero h1 {
      font-size: 3.2rem;
      margin: 15px 0;
      color: var(--secondary-gold);
    }

    .hero p.tagline {
      font-style: italic;
      color: var(--text-muted);
      margin-bottom: 25px;
    }

    .countdown-container {
      display: flex;
      gap: 15px;
      margin-top: 30px;
    }

    .time-box {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid var(--primary-gold);
      border-radius: 8px;
      padding: 15px 20px;
      min-width: 75px;
      backdrop-filter: blur(5px);
    }

    .time-box span {
      display: block;
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary-gold);
    }

    .time-box label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-muted);
    }

    section {
      padding: 80px 20px;
      border-bottom: 1px solid rgba(212, 175, 55, 0.1);
    }

    .section-title {
      font-size: 2rem;
      color: var(--primary-gold);
      margin-bottom: 10px;
    }

    .couple-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 30px;
      margin-top: 40px;
    }

    .couple-card {
      background: var(--card-bg);
      border: 1px solid rgba(212, 175, 55, 0.2);
      border-radius: 12px;
      padding: 30px;
      max-width: 380px;
      flex: 1 1 300px;
    }

    .couple-card img {
      width: 140px;
      height: 140px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid var(--primary-gold);
      margin-bottom: 15px;
    }

    .couple-card h3 {
      font-size: 1.6rem;
      color: var(--secondary-gold);
      margin-top: 10px;
    }

    .couple-card span {
      display: block;
      color: var(--primary-gold);
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 15px;
    }

    .story-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 20px;
      margin-top: 40px;
    }

    .story-card {
      background: var(--card-bg);
      padding: 25px;
      border-radius: 8px;
      border-left: 3px solid var(--primary-gold);
      text-align: left;
    }

    .story-card h4 {
      color: var(--secondary-gold);
      margin-bottom: 8px;
    }

    .events-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 25px;
      margin-top: 40px;
    }

    .event-card {
      background: var(--card-bg);
      border: 1px solid rgba(212, 175, 55, 0.3);
      border-radius: 10px;
      padding: 30px;
      width: 300px;
      position: relative;
    }

    .event-card h4 {
      font-size: 1.3rem;
      color: var(--primary-gold);
      margin-bottom: 5px;
    }

    .event-time {
      font-weight: 600;
      color: var(--secondary-gold);
      margin: 10px 0;
    }

    .event-venue {
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    footer {
      padding: 40px 20px;
      background: #090a0e;
      color: var(--text-muted);
      font-size: 0.85rem;
    }

    @media (max-width: 600px) {
      .hero h1 { font-size: 2.2rem; }
      .cursive { font-size: 2.2rem; }
      .countdown-container { gap: 8px; }
      .time-box { padding: 10px 12px; min-width: 60px; }
      .time-box span { font-size: 1.3rem; }
    }
  </style>
</head>
<body>

  <!-- HERO SECTION -->
  <header class="hero">
    <h3>A Celebration of Love</h3>
    <h1>Harshit & Kavita</h1>
    <p class="cursive">Two hearts, one love, forever together</p>
    <p class="tagline">Save the Date — 25th November, 2026</p>

    <!-- COUNTDOWN TIMER -->
    <div class="countdown-container">
      <div class="time-box"><span id="days">00</span><label>Days</label></div>
      <div class="time-box"><span id="hours">00</span><label>Hours</label></div>
      <div class="time-box"><span id="minutes">00</span><label>Mins</label></div>
      <div class="time-box"><span id="seconds">00</span><label>Secs</label></div>
    </div>
  </header>

  <!-- MEET THE COUPLE -->
  <section class="container">
    <h2 class="section-title">Meet the Couple</h2>
    <p class="cursive">Two souls, one destiny</p>
    
    <div class="couple-grid">
      <div class="couple-card">
        <!-- Replace src link with Harshit's photo link -->
        <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=400&q=80" alt="Harshit">
        <h3>Harshit</h3>
        <span>The Groom</span>
        <p>A simple, warm-hearted soul ready to start the best chapter of his life with Kavita.</p>
      </div>

      <div class="couple-card">
        <!-- Replace src link with Kavita's photo link -->
        <img src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&w=400&q=80" alt="Kavita">
        <h3>Kavita</h3>
        <span>The Bride</span>
        <p>Full of grace, warmth, and laughter, bringing endless joy to everyone around her.</p>
      </div>
    </div>
  </section>

  <!-- OUR LOVE STORY -->
  <section class="container">
    <h2 class="section-title">Our Love Story</h2>
    <p style="color: var(--text-muted)">A journey that brought two hearts together</p>

    <div class="story-grid">
      <div class="story-card">
        <h4>✨ How We Met</h4>
        <p>A beautiful beginning that felt meant to be from the very first conversation.</p>
      </div>
      <div class="story-card">
        <h4>☕ The First Date</h4>
        <p>Conversations flowed effortlessly over tea, turning hours into moments.</p>
      </div>
      <div class="story-card">
        <h4>💛 Falling in Love</h4>
        <p>Through endless laughter, shared dreams, and quiet understanding, love blossomed.</p>
      </div>
      <div class="story-card">
        <h4>💍 The Proposal</h4>
        <p>A heartfelt promise to walk hand-in-hand through life's adventures forever.</p>
      </div>
    </div>
  </section>

  <!-- WEDDING EVENTS -->
  <section class="container">
    <h2 class="section-title">Wedding Events</h2>
    <p style="color: var(--text-muted)">Join us in our grand celebrations</p>

    <div class="events-grid">
      <div class="event-card">
        <h4>Haldi Ceremony</h4>
        <p style="font-size: 0.85rem; color: var(--primary-gold)">Turmeric Blessing</p>
        <p class="event-time">24th Nov 2026 | Morning</p>
        <p class="event-venue">Venue Details Coming Soon</p>
      </div>

      <div class="event-card">
        <h4>Wedding / Muhurtham</h4>
        <p style="font-size: 0.85rem; color: var(--primary-gold)">The Sacred Union</p>
        <p class="event-time">25th Nov 2026 | Evening</p>
        <p class="event-venue">Venue Details Coming Soon</p>
      </div>

      <div class="event-card">
        <h4>Reception</h4>
        <p style="font-size: 0.85rem; color: var(--primary-gold)">Celebration & Dinner</p>
        <p class="event-time">26th Nov 2026 | 7:00 PM Onwards</p>
        <p class="event-venue">Venue Details Coming Soon</p>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>Harshit & Kavita • Made with love for a celebration of love 💛</p>
  </footer>

  <!-- JAVASCRIPT FOR COUNTDOWN -->
  <script>
    // WEDDING DATE SET TO 25TH NOV 2026 AT 10:00 AM IST
    const weddingDate = new Date("2026-11-25T10:00:00").getTime();

    function updateCountdown() {
      const now = new Date().getTime();
      const distance = weddingDate - now;

      if (distance < 0) {
        document.querySelector(".countdown-container").innerHTML = "<p style='color: var(--primary-gold); font-size: 1.5rem;'>The Celebration Has Begun! 🎉</p>";
        return;
      }

      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);

      document.getElementById("days").innerText = days < 10 ? "0" + days : days;
      document.getElementById("hours").innerText = hours < 10 ? "0" + hours : hours;
      document.getElementById("minutes").innerText = minutes < 10 ? "0" + minutes : minutes;
      document.getElementById("seconds").innerText = seconds < 10 ? "0" + seconds : seconds;
    }

    setInterval(updateCountdown, 1000);
    updateCountdown();
  </script>
</body>
</html>